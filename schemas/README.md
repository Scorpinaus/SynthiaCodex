# Codex app-server schemas

These files are generated from **Codex CLI 0.147.0**, which is SynthiaCode's current protocol baseline.

Regenerate them reproducibly from the repository root with the pinned CLI:

```powershell
npx --yes @openai/codex@0.147.0 app-server generate-json-schema --experimental --out schemas
```

Do not hand-edit the generated JSON files. Update this baseline note and the schema contract test whenever the pinned Codex CLI baseline changes.
