<UserControl x:Class="NativeCodexAssistant.App.Views.TerminalView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource Card}" Margin="0,10,0,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="PowerShell" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="{Binding Terminal.WorkingDirectory}" Foreground="{DynamicResource MutedInkBrush}" TextTrimming="CharacterEllipsis"/></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="Start" Command="{Binding Terminal.StartCommand}" Style="{StaticResource CompactButton}" Margin="0,0,6,0"/><Button Content="Clear" Command="{Binding Terminal.ClearCommand}" Style="{StaticResource CompactButton}" Margin="0,0,6,0"/><Button Content="Kill" Command="{Binding Terminal.KillCommand}" Style="{StaticResource CompactButton}"/></StackPanel></Grid>
        <TextBox Grid.Row="1" Text="{Binding Terminal.Output, Mode=OneWay}" IsReadOnly="True" FontFamily="{StaticResource MonoFont}" FontSize="12" AcceptsReturn="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Padding="10" Margin="0,12"/>
        <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding Terminal.Input, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource MonoFont}" Padding="9"/><Button Grid.Column="1" Content="Send" Command="{Binding Terminal.SendInputCommand}" Style="{StaticResource PrimaryButton}" MinWidth="86" Margin="8,0,0,0"/></Grid>
    </Grid></Border>
</UserControl>
